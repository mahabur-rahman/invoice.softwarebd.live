version: "3.9"

services:
  nextjs:
    image: node:22-alpine
    container_name: nextjs-dev
    working_dir: /app

    volumes:
      - .:/app
      - /app/node_modules

    ports:
      - "3000:3000"

    env_file:
      - .env.local

    command: sh -c "npm install && npm run dev"
